
function sumAll(arr) {
  
  var lower = 0;
  var higher = 0;
  if (arr[0] > arr[1]) {
    lower = arr[1];
    higher = arr[0];
  } else {
    lower = arr[0];
    higher = arr[1];
  }
  var sum = 0;
  for (var i= lower; i <= higher; i++){
    sum += i;
  }
  return sum;
}

sumAll([1, 4]);
